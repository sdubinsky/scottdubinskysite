<!DOCTYPE HTML>
<html>
    <head>
        <title>Shalom Dubinsky</title>
        <link rel="stylesheet" type="text/css" href="<%=url("css/index.css") %>" />
        <link rel="stylesheet" type="text/css" href="<%=url("css/layout.css")%>" />
    </head>
    <body>
        <header class="header">
            <div class="header-content">
                <svg class="header-svg" >
                    <path d="M0 100% Q50% 0 100% 100%" stroke="black" fill="transparent" />
                    <circle cx="0" cy="100%" r="3"/>
                    <circle cx="50%" cy="0" r="3"/>
                    <circle cx="100%" cy="100%" r="3" />
                </svg>
                <div id="navbar">
                    <ul class="navbar-list">
                        <% navbar_items = ["Home", "Projects"] %>
                        <% navbar_items.each do |item| %>
                            <li class="navbar-list-item">
                                <% if item.downcase == current_page %>
                                    <a class="nav-link active" href="/<%= item.downcase %>"><%= item %></a>
                                <% else %>
                                    <a class="nav-link" href="/<%= item.downcase %>"><%= item %></a>
                                <% end %>
                            </li>
                        <% end %>
                        <li class="navbar-list-item">
                            <span class="name">
                                <img id="logo" src="img/fencinglungebannersmall.png" alt="Shalom Dubinsky" />
                            </span>
                        </li>
                        <% navbar_items = ["Resume", "Fencing"] %>
                        <% navbar_items.each do |item| %>
                            <li class="navbar-list-item">
                                <% if item.downcase == current_page %>
                                    <a class="nav-link active" href="/<%= item.downcase %>"><%= item %></a>
                                <% else %>
                                    <a class="nav-link" href="/<%= item.downcase %>"><%= item %></a>
                                <% end %>
                        <% end %>
                    </ul>
                </div>
            </div>
        </header>
        <div class="content">
            <%= yield %>
        </div>
        <footer class="footer">
            <div class="footer-links">
                <a href="https://linkedin.com/in/sdubinsky" target="_blank" style="text-decoration:none;"><img src="https://static.licdn.com/scds/common/u/img/webpromo/btn_in_20x15.png" width="20" height="15" alt="View my LinkedIn profile" style="vertical-align:middle;" border="0"></a>
                <a href="https://github.com/sdubinsky" target="_blank"><img src="img/github-mark-32px.png" alt="View my Github profile" style="vertical-align:middle;" height="20" width="20"/></a>
            </div>
        </footer>
    </body>
</html>
